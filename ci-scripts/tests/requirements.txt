# SPDX-License-Identifier: MIT
#
# Runtime dependencies for the UPF integration test suite.
#
# scapy supplies the PFCP protocol model (scapy.contrib.pfcp). Pinned because the
# suite depends on concrete IE field names, and those have shifted between
# releases (e.g. IE_FTEID uses `V4`/`TEID` while IE_FSEID uses `v4`/`seid`).
# Verified against 2.7.0.
scapy==2.7.0

# pfcpkit is the UPF-agnostic PFCP machinery this suite is built on --
# protocol client, scenario lifecycle, generic inspectors. It lives in its own
# repo now: https://github.com/openairinterface/pfcp-kit.
#
# git+ssh, not git+https: the repo is currently private, so pip needs your SSH
# key (the same one `git clone git@github.com:...` already uses) rather than a
# token. Switch back to git+https once the repo is public, if preferred.
#
# Pinned to a branch until pfcp-kit cuts its first tagged release; swap the ref
# for a tag at that point (e.g. `@v0.1.0`) so this suite stops moving with
# pfcp-kit's default branch.
pfcpkit @ git+ssh://git@github.com/openairinterface/pfcp-kit.git@feat-initial-import
